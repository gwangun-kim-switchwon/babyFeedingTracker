<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BabyFeedingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">

        <!-- Pretendard 다운로드 폰트 프리로드 (R9 §5 Typography) -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.App.Starting">
            <!-- Compose handles theming via BabyFeedingTrackerTheme -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 기존 위젯 (v2.0) — 사용자 데이터 보호 위해 유지, v2.2에서 폐기 검토 -->
        <receiver
            android:name=".ui.widget.FeedingWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/feeding_widget_info" />
        </receiver>

        <!-- v2.1 신규 반응형 독립 위젯 -->
        <receiver
            android:name=".ui.widget.MammamiaWidgetReceiver"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/mammamia_widget_info" />
        </receiver>

        <!-- v2.1 수유 리마인더 알림 액션 수신기 -->
        <receiver
            android:name=".notification.FeedingReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.baby.feedingtracker.action.LOG_FEEDING" />
                <action android:name="com.baby.feedingtracker.action.SNOOZE_30" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
